<%= link_to gravatar_for(current_user, size: 60), current_user %>
<span><%= current_user.name %></span>
<%= link_to "view my profile", current_user %>
<span>Total post
  <span class="badge"><%= current_user.microposts.count %></span>
</span>
